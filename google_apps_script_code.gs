/**
 * ==============================================================================
 * ATENÇÃO / NOTICE:
 * Este arquivo (google_apps_script_code.gs) foi unificado com /Codigo.gs.
 *
 * A fonte única e oficial de código do backend Google Apps Script fica em:
 * -> /Codigo.gs
 *
 * Qualquer alteração no código do Apps Script deve ser feita EXCLUSIVAMENTE
 * em /Codigo.gs. O front-end (src/lib/appsScriptCode.ts) importa o arquivo
 * /Codigo.gs diretamente como raw text em tempo de build/execução.
 * ==============================================================================
 */
